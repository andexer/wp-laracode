<!DOCTYPE html>
<html lang="{{-- wp_get_locale() --}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@yield('title', '{{pluginName}}')</title>

    @stack('styles')
</head>
<body class="{{slug}}-app">
    <div id="{{slug}}-container">
        @yield('content')
    </div>

    {{-- Livewire Scripts --}}
    @livewireScripts

    @stack('scripts')
</body>
</html>
